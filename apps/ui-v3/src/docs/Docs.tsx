/**
 * Docs — repo file tree + the serif reader pane.
 *
 * This file is only the mount point `routes.tsx`'s surface glob resolves
 * (`SURFACE_ROOTS.docs`, which the shell owns). The actual surface lives in
 * `../docsview/` - see `DocsView.tsx` for the file this component is.
 */
export { default } from "../docsview/DocsView.tsx";
